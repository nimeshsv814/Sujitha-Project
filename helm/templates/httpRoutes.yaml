apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sujitha-route
  namespace: suji-ns
spec:
  parentRefs:
    - name: suji-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: sujitha-svc
          port: 3000